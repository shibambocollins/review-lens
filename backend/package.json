{
  "name": "review-lens-backend",
  "version": "1.0.0",
  "description": "Backend API for ReviewLens - AI review intelligence platform. Proxies AI calls (Gemini -> Groq -> Cloudflare Workers AI fallback chain) so provider keys never reach the browser.",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
